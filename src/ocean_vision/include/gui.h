#include "common.h"
#include "CMT.h"

void screenLog(Mat im_draw, const string text);
Rect getRect(const Mat im, const string win_name);
int display_ros(Mat im, cmt::CMT & cmt, const string win_name);

